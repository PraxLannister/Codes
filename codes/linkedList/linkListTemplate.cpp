#include<iostream>
template<typename T>
class linkList
{
	T data;
	linkList *next;
public:
	linkList(){
		this->next = NULL;
	};
	void isertNode(linkList **head,T data){
		linkList *newNode = new createNode();
		if(*head == NULL)

	}

	
};